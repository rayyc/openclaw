// Paystack client placeholder (replaced Stripe/Paddle)
// Frontend does not perform sensitive vendor auth — backend handles secret keys.
// If you want to integrate Paystack's inline checkout, use `NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY`.